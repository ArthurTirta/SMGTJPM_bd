# =====================================================
# MINIMAL REQUIREMENTS FOR AI CHAT BACKEND
# =====================================================
# Size: ~200-300 MB (vs 3-4 GB original)
# Docker build time: ~2-3 min (vs 10-15 min original)
# =====================================================

# Core FastAPI Framework
fastapi==0.115.0
uvicorn==0.30.6
pydantic==2.9.0
pydantic-settings==2.5.0
python-multipart==0.0.6
alembic ==1.18.3

# Database (PostgreSQL)
sqlalchemy==2.0.31
psycopg2-binary==2.9.9

# AI/ML - Google Gemini
google-genai==1.62.0

# Configuration & Environment
python-dotenv==1.0.1

# Utilities
rich==14.2.0
boto3==1.34.0
pillow==11.3.0



SQLAlchemy==2.0.31
typer==0.19.2
typing-inspection==0.4.1
typing_extensions==4.15.0


# =====================================================
# OPTIONAL PACKAGES (Uncomment if needed)
# =====================================================

# Email validation (if you validate emails)
# email-validator==2.3.0
# dnspython==2.8.0

# Error tracking & monitoring
# sentry-sdk==2.51.0

# Analytics
# posthog==5.4.0

# HTTP client (if you make external API calls)
# httpx==0.28.1

# Password hashing (if you have user authentication)
# bcrypt==5.0.0

# =====================================================
# PLATFORM-SPECIFIC (Do not include in Docker)
# =====================================================
# pywin32==311; sys_platform == 'win32'
# colorama==0.4.6; sys_platform == 'win32'